{
  "name": "auto-swe",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "yarn workspaces foreach -At run build",
    "typecheck": "yarn workspace @auto-swe/shared run build && yarn workspaces foreach -A run typecheck",
    "dev:gateway": "yarn workspace @auto-swe/gateway dev",
    "dev:worker": "yarn workspace @auto-swe/worker dev",
    "dev:web": "yarn workspace @auto-swe/web dev",
    "db:migrate": "yarn workspace @auto-swe/shared prisma migrate dev",
    "db:generate": "yarn workspace @auto-swe/shared prisma generate",
    "db:seed": "yarn workspace @auto-swe/shared prisma db seed",
    "db:studio": "yarn workspace @auto-swe/shared prisma studio",
    "docker:infra": "docker compose -f docker-compose.infra.yml ps",
    "docker:infra:up": "docker compose -f docker-compose.infra.yml up -d",
    "docker:infra:down": "docker compose -f docker-compose.infra.yml down",
    "docker:up": "docker compose -f docker-compose.infra.yml -f docker-compose.yml up -d",
    "docker:down": "docker compose -f docker-compose.infra.yml -f docker-compose.yml down",
    "docker:logs": "docker compose -f docker-compose.infra.yml -f docker-compose.yml logs -f",
    "docker:build": "docker compose -f docker-compose.infra.yml -f docker-compose.yml build",
    "test": "vitest run",
    "test:watch": "vitest",
    "lint": "biome check .",
    "lint:fix": "biome check . --write",
    "format": "biome format . --write"
  },
  "engines": {
    "node": ">=24.0.0"
  },
  "devDependencies": {
    "@biomejs/biome": "2.4.14",
    "tsx": "4.21.0",
    "vitest": "4.1.5"
  },
  "packageManager": "yarn@4.14.1"
}
