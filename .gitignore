node_modules/
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/sdks
!.yarn/versions
dist/
.env
*.tsbuildinfo

# Prisma generated client — regenerated via `yarn db:generate`
packages/shared/src/generated/

# npm lockfile — this is a Yarn 4 project
package-lock.json

# Next.js
.next/
# Regenerated by `next dev` / `next build` with different content per mode — track noise not worth it
packages/web/next-env.d.ts

# AI
.claude/settings.local.json
.claude/*.lock
.claude/worktrees/
tmp/

.superpowers/
docs/superpowers/

# Generated codebase reference artifacts
output/

# Docker bind-mount data directories (Postgres, Temporal Postgres)
data/
test-results/
.worktrees/

# vitest coverage output
coverage/

# Env files: ignore everything except the committed examples.
# `.env.*` does not match `.env` itself, hence both lines. Negations come last
# so they win regardless of what is above.
.env
.env.*
!.env.example
!.env.*.example

# Editor config: share project settings, ignore everything else VS Code writes.
.vscode/*
!.vscode/settings.json
!.vscode/extensions.json
!.vscode/launch.json
!.vscode/tasks.json
