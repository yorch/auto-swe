

# Local infra state (the docker:infra postgres/minio bind mounts). Root-owned by
# the container, so sending it as build context both bloats the context and can
# fail outright with "error from sender: ... permission denied".
data/
